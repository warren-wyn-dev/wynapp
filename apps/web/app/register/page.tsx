'use client'; import {useState} from 'react'; export default function Register(){const[s,setS]=useState('idle');return <section className="card"><h1>สร้างบัญชี</h1><form className="grid" onSubmit={async e=>{e.preventDefault();setS('loading');const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000'}/v1/auth/register`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.email,password:f.password,username:f.username,displayName:f.displayName})});setS(r.ok?'success':'error')}}><label>ชื่อที่แสดง<input name="displayName" maxLength={50} required/></label><label>ชื่อผู้ใช้<input name="username" minLength={3} maxLength={30} pattern="[A-Za-z0-9_]+" required/></label><label>อีเมล<input name="email" type="email" required/></label><label>รหัสผ่าน<input name="password" type="password" minLength={12} required/></label><button disabled={s==='loading'}>{s==='loading'?'กำลังสร้าง…':'สมัครสมาชิก'}</button>{s==='success'&&<p role="status">ตรวจสอบอีเมลเพื่อยืนยันบัญชี</p>}{s==='error'&&<p role="alert">ไม่สามารถสมัครได้ โปรดตรวจสอบข้อมูล</p>}</form></section>}
