import { describe, expect, it } from 'vitest';
import { canApproveFollowRequest, canBlock, canCancelFollowRequest, canFollow, canMute, canRemoveFollower, canViewProfile } from '../../packages/social/src/policies.js';
describe('central social policies',()=>{const facts={viewerId:'a',targetId:'b',blockedEitherWay:false,accountVisibility:'PRIVATE' as const,viewerFollowsTarget:false};it('denies self and blocked follow/block/mute',()=>{expect(canFollow({...facts,targetId:'a'})).toBe(false);expect(canFollow({...facts,blockedEitherWay:true})).toBe(false);expect(canBlock('a','a')).toBe(false);expect(canMute('a','a')).toBe(false)});it('enforces private profile access',()=>{expect(canViewProfile(facts)).toBe(false);expect(canViewProfile({...facts,viewerFollowsTarget:true})).toBe(true)});it('binds request and follower actions to owners',()=>{expect(canApproveFollowRequest('a','b')).toBe(false);expect(canCancelFollowRequest('a','a')).toBe(true);expect(canRemoveFollower('a','b')).toBe(false)})});
