# Quote ReDrop

A quote stores its own plain text and author plus `original_drop_id`; it never copies authoritative original content. Creation locks and re-authorizes the published original. A renderer must re-authorize the original at read time and show an unavailable placeholder after deletion or visibility loss, without cached original content.
