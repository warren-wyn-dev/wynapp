# Comments

Comments are plain text of 1–2,000 Unicode characters. The server rejects control characters and unknown identity/count fields; React renders text with contextual escaping. Authors may soft-delete their own comments, which become tombstones. Editing is intentionally deferred because no Founder-approved edit window exists.
