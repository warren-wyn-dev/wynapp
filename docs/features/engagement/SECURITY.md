# Engagement security

All direct endpoints deny unavailable resources without disclosing whether deletion, privacy or a bidirectional block caused the result. Client author IDs and counters are not accepted. Saves remain owner-only. Original content is referenced, never duplicated by ReDrops. Comment text remains plain text and must be rendered through framework escaping; HTML is not trusted.
