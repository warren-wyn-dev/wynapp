# Moderation foundation

Club bans, member removal, role changes, rule changes, pins, and content removal use granular role checks, require a reason for sensitive actions, and append immutable `club_audit_events`. Owner targets are protected. The global moderation center remains Step 15 and is not implemented here.
