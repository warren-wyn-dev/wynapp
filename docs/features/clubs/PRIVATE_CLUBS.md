# Private Clubs

Non-members receive only safe identity metadata: name, slug, visibility, media identifiers and member count. The API denies feed, trending, member-list, and content access unless membership exists. Search omits private descriptions. All checks run server-side.
