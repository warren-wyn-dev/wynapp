# Club feed

The feed is keyset paginated by publication time, filters published/non-deleted Drops by Club, and puts pinned Drops first. `newest` is default; `popular` orders by scoped likes. V1 allows at most three pins (enforced by service transaction before insertion).
