# Relationship privacy

Private profile details and relationship lists are visible only to the owner and approved followers. Public-safe identity, visibility marker, and counts remain available when not blocked. Authenticated profile responses expose only viewer-relative `isFollowing`, `followsYou`, `followRequestPending`, `canFollow`, and the viewer's outgoing `isMuted`; the inverse mute state is never returned.
