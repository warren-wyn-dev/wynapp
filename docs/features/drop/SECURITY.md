# Drop security review

Author identity always comes from the authenticated session. Zod and database checks reject oversized content, unsafe URL schemes, duplicate/excess media, and invalid polls. React renders text escaped; line breaks use CSS. CSRF, stable errors, request IDs, rate limits, ownership, block/privacy checks, row/advisory locks, idempotency, revisions, and outbox writes cover the Step 9 trust boundaries. No CRITICAL or HIGH issue was found in self-review.
