# Visibility

Drafts are author-only. Published reads deny either-direction blocks. `FOLLOWERS` requires an accepted `follows` row; a private account also requires that row even for a `PUBLIC` Drop. Owners retain access. Unauthorized direct-ID reads return not-found to minimize enumeration.
