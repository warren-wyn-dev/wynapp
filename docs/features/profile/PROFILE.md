# Profile

Profiles contain username, display name, plain-text bio, validated website, optional location, nullable avatar/cover references, and creation time. Media remains null until the Step 8 pipeline. Public API allowlists these fields and never returns email, credentials, sessions, or internal account state.
